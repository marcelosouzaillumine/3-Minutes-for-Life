{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build",
  "rewrites": [
    { "source": "/r/:code", "destination": "/api/share?code=:code" },
    { "source": "/(.*)",    "destination": "/index.html" }
  ]
}
